Verifique o estado atual do projeto A3 em /home/enzocabrera/UNA/Computacao Grafica/A3.

Execute as seguintes verificações e reporte de forma compacta:

1. Liste os arquivos .py existentes na raiz e indique quais módulos já foram criados (composer, preprocessor, database, features, recognizer, main).
2. Verifique se a pasta `faces/` existe e quais subpastas de componentes têm arquivos PNG.
3. Verifique se a pasta `database/` existe e tem imagens.
4. Verifique se `output/retrato_falado_suspeito.png` existe.
5. Verifique se `model/lbph_model.yml` existe.

Com base no CLAUDE.md (etapas 1-10), indique quais etapas estão concluídas, em andamento e pendentes.

Formato da resposta: lista compacta, sem explicações longas.
